<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:commons="clr-namespace:SmartConstructionServices.Common;assembly=SmartConstructionServices"
             x:Class="SmartConstructionSite.Account.LoginPage">
    <ContentPage.Resources>
        <ResourceDictionary>
            <commons:NotConverter x:Key="notConverter"></commons:NotConverter>
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.Content>
        <StackLayout Orientation="Vertical">
            <ActivityIndicator IsRunning="{Binding LoginPending}"></ActivityIndicator>
            <Image Source="icon.png" HorizontalOptions="Center" VerticalOptions="Center"></Image>
            <Entry Placeholder="用户名" VerticalOptions="Center" Text="{Binding Username}" IsEnabled="{Binding Path=LoginPending, Converter={StaticResource notConverter}}"></Entry>
            <Entry Placeholder="密码" IsPassword="True" VerticalOptions="Center" Text="{Binding Password}" IsEnabled="{Binding Path=LoginPending, Converter={StaticResource notConverter}}"></Entry>
            <Button Text="登录" VerticalOptions="Center" Command="{Binding LoginCommand}"></Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>